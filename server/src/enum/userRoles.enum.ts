export enum UserRole {
  Teacher = "teacher",
  Student = "student",
  Admin = "admin",
}

export enum UserStatus {
  Active = "active",
  Inactive = "inactive",
  Banned = "banned",
  Deleted = "deleted",
  Suspended = "suspended",
}

export enum UserGender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum UserLanguages {
  English = "en-US",
  Turkish = "tr-TR",
}
